# Open Z39.50 Gateway Configuration

# Server Ports
PORT=8899
ZSERVER_PORT=2100

# Database Provider: sqlite or postgres
DB_PROVIDER=sqlite

# Security
GATEWAY_API_KEY=your_secret_api_key_here

# Access Control (Comma separated list of allowed IP/CIDR)
# Default is 0.0.0.0/0 (allow all) for demo. 
# Change this to your specific IP for security!
ZSERVER_ALLOWED_IPS=0.0.0.0/0

# Postgres config (only used if DB_PROVIDER=postgres)
POSTGRES_PASSWORD=your_secure_db_password
